PORT=4000
MONGODB_URI=mongodb://localhost:27017/family_comes_first
JWT_SECRET=change_me_in_production
JWT_EXPIRES_IN=7d
CLIENT_ORIGIN=http://localhost:5173
NODE_ENV=development
# Leave OPENROUTER_MODEL unset to use a free OpenRouter key's default model
# (no "model" field is sent). Set it (e.g. anthropic/claude-opus-5) to pin a
# specific paid model instead.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=
